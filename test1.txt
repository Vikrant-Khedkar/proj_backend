contract.methods.addRecord(
    30, // Age
    1672531200, // Date of Visit (Assuming Unix timestamp format)
    "Diagnosis",
    "Prescription",
    100, // Fees
    1682531200, // Follow-up (Assuming Unix timestamp format)
    120, // Blood Pressure
    70 // Weight
).send({ from: senderAccount.address, gas: 2000000 })
.on('transactionHash', (hash) => {
    console.log('Transaction Hash:', hash);
})
.on('receipt', (receipt) => {
    console.log('Transaction Receipt:', receipt);
})
.on('error', (error) => {
    console.error('Error:', error);
});
